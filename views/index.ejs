<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Weather Application</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
    crossorigin="anonymous">
  <link rel="stylesheet" href="style.css"> <!-- Link to your custom stylesheet -->
</head>

<body >
  <div class="container mt-5">
    <div class="row justify-content-center">
      <div class="col-md-6">
        <div class="card border-info">
          <div class="card-body text-info">
            <h1 class="card-title text-center mb-4">Weather Application</h1>
            <h4 class="card-title text-center mb-4">Get Your Weather For Desired Location</h4>
            <form id="weatherForm" action="/weather" method="get">
              <div class="form-group">
                <label for="cityName">Enter City Name:</label>
                <input type="text" class="form-control" id="cityName" name="city" placeholder="E.g., New York"
                  required>
              </div>
              <button type="submit" class="btn btn-primary btn-block mt-3">Get Weather</button>
            </form>
            <% if (error) { %>
            <p class="error mt-3"><%= error %></p>
            <% } else if (weather) { %>
            <div class="card bg-info text-dark p-3">
              <img src="https://openweathermap.org/img/wn/<%= weather.weather[0].icon %>@2x.png"
                alt="<%= weather.weather[0].description %>" class="img-fluid weather-icon">
              <h4 class="mt-3"><%= weather.name %>, <%= weather.sys.country %></h4>
              <p class="weather-description"><%= weather.weather[0].description %></p>
              <p class="temperature">Temperature: <%= weather.main.temp %> &deg;C</p>
              <p class="humidity">Humidity: <%= weather.main.humidity %> %</p>
              <p class="wind-speed">Wind Speed: <%= weather.wind.speed %> m/s</p>
            </div>
            <% } %>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>

</html>
